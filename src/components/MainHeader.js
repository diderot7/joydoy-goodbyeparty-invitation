import React from "react";

const MainHeader = ({ text }) => {
  return (
    <div className="MainHeader">
      <div>
        <h4>{text}</h4>
      </div>
    </div>
  );
};

export default MainHeader;

// 세젤귀 전도영군의 잘가라(가지마)파티에 초대합니다.
